import { styled } from '../../styles'

const Root = styled('span', {
  fontWeight: '$bold',
  fontSize: '$14',
  color: '$gray200',
  backgroundColor: '$gray400',
  padding: '2px 8px',
  borderRadius: '9999px',
})

export default {
  Root,
}
