import { styled } from '../../styles'

const Root = styled('i', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  minWidth: 'fit-content',
  minHeight: 'fit-content',
  cursor: 'pointer',
  fontSize: '$18',
})

export default {
  Root,
}
